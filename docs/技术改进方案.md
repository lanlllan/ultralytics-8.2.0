# YOLOv8n-seg 快递单检测技术改进方案（统一文档）

> **统一技术参考** — 整合已完成的注意力机制改进、已完成的损失函数改进（IoU + 掩码）、完整的实验工具链
>
> 目标模型：YOLOv8n-seg | 任务：单类运单（waybill）实例分割 | 目的：学术论文

---

## 一、项目背景

### 1.1 任务描述

| 项目 | 说明 |
|------|------|
| 任务类型 | 实例分割（Instance Segmentation） |
| 检测目标 | 快递运单（单类别，nc=1） |
| 基线模型 | YOLOv8n-seg（3.41M 参数，~12.6 GFLOPs） |
| 数据规模 | 小数据集（约 330 训练图 / 99 验证图） |
| 训练硬件 | NVIDIA RTX 3060 Laptop 6GB |
| 输入分辨率 | 960×960 |
| 核心目标 | 提升 mAP@0.5:0.95，用于学术论文 |

### 1.2 运单检测任务特点

- **形状不规则**：运单可能折叠、弯曲、部分遮挡，边缘轮廓复杂
- **尺度变化大**：近景占满画面，远景仅占图像一小部分
- **背景干扰**：运单放置在包裹、桌面、传送带等复杂背景上
- **边缘模糊**：胶带覆盖、褶皱、光照不均导致边界不清晰

### 1.3 基线模型架构

配置文件：`ultralytics/cfg/models/v8/yolov8-seg.yaml`，缩放参数 `n: [0.33, 0.25, 1024]`

```
Backbone（特征提取）
──────────────────────────────────────────────────
层号  模块         通道(实际)  输出尺寸(960输入)  说明
 0   Conv         3→16      480×480          P1/2
 1   Conv         16→32     240×240          P2/4
 2   C2f ×1       32→32     240×240
 3   Conv         32→64     120×120          P3/8
 4   C2f ×2       64→64     120×120          ──→ Neck
 5   Conv         64→128    60×60            P4/16
 6   C2f ×2       128→128   60×60            ──→ Neck
 7   Conv         128→256   30×30            P5/32
 8   C2f ×1       256→256   30×30
 9   SPPF         256→256   30×30            Backbone 末尾

Neck + Head（FPN 特征融合 + 检测）
──────────────────────────────────────────────────
10   Upsample     256       60×60
11   Concat       384       60×60            +P4(#6)
12   C2f ×1       128       60×60
13   Upsample     128       120×120
14   Concat       192       120×120          +P3(#4)
15   C2f ×1       64        120×120          P3 输出
16   Conv         64        60×60            ↓2
17   Concat       192       60×60            +#12
18   C2f ×1       128       60×60            P4 输出
19   Conv         128       30×30            ↓2
20   Concat       384       30×30            +P5(#9)
21   C2f ×1       256       30×30            P5 输出
22   Segment      (P3,P4,P5)                 32 proto, 256 ch
```

### 1.4 当前损失函数

`v8SegmentationLoss`（继承 `v8DetectionLoss`）计算 4 项损失：

| 损失项 | 计算方式 | 权重 | 代码位置 |
|--------|---------|------|---------|
| Box Loss | CIoU Loss | ×7.5 | `loss.py` L76 |
| Seg Loss | BCE（逐像素掩码） | ×7.5 | `loss.py` L364 |
| Cls Loss | BCE（类别） | ×0.5 | `loss.py` L309 |
| DFL Loss | Distribution Focal Loss | ×1.5 | `loss.py` L82 |

---

## 二、改进技术一：注意力机制（已完成实施与实验）

> 详细分析文档：`docs/attention-analysis.md`
> 论文写作参考：`docs/attention-integration-doc.md`
> 训练脚本：`cmd/yolov8-seg-attention-train.py`
> 评估脚本：`cmd/yolov8-seg-attention-val.py`

### 2.1 实验采用的注意力模块

代码中共实现了 5 个注意力类（`conv.py`），但实际实验选取了 **3 种**具有代表性的机制，覆盖不同注意力维度：

| 模块 | 来源 | 注意力维度 | 可学习参数(C=256) | 核心特点 | 实验状态 |
|------|------|----------|----------------|---------|---------|
| **CBAM** | Woo et al., ECCV 2018 | 通道+空间 | ~65,890 | 双维度串联，经典稳定 | **已实验** |
| **SimAM** | Yang et al., ICML 2021 | 3D（通道+空间） | **0** | 完全无参数，基于能量函数 | **已实验** |
| **CoordAtt** | Hou et al., CVPR 2021 | 通道+位置 | ~6,160 | 水平/垂直位置编码，边缘敏感 | **已实验** |
| SEAttention | Hu et al., CVPR 2018 | 通道 | ~8,200 | 挤压激励，FC 瓶颈降维 | 代码备用 |
| ECAAttention | Wang et al., CVPR 2020 | 通道 | ~5 | 1D 卷积替代 FC，极轻量 | 代码备用 |

**选型理由**：
- **CBAM**：通道+空间双维度，经典基线
- **SimAM**：零参数消融对照——若零参数也能提升，说明注意力的"特征选择"本身有效
- **CoordAtt**：位置编码对运单边缘分割尤其有利

实验采用 **2 个插入位置**：Position A（SPPF 后，1 层）和 Position C（Neck 每个 C2f 后，4 层），共 **5 组注意力实验 + 1 组基线对照**。

#### CBAM 实现

```python
# conv.py L313-L324
class CBAM(nn.Module):
    def __init__(self, c1, kernel_size=7):
        super().__init__()
        self.channel_attention = ChannelAttention(c1)
        self.spatial_attention = SpatialAttention(kernel_size)

    def forward(self, x):
        return self.spatial_attention(self.channel_attention(x))
```

公式：$\hat{F} = F \otimes \sigma(\text{Conv}_{1 \times 1}(\text{GAP}(F))) \otimes \sigma(\text{Conv}_{7 \times 7}([\text{Avg}; \text{Max}]))$

#### SimAM 实现（零参数）

```python
# conv.py L362-L375
class SimAM(nn.Module):
    def __init__(self, c1, e_lambda=1e-4):
        super().__init__()
        self.e_lambda = e_lambda

    def forward(self, x):
        b, c, h, w = x.size()
        n = h * w - 1
        mean = x.mean(dim=[2, 3], keepdim=True)
        var = ((x - mean) ** 2).sum(dim=[2, 3], keepdim=True) / n
        y = (x - mean) / (var + self.e_lambda).sqrt()
        return x * y.sigmoid()
```

公式：$\hat{x} = x \odot \sigma\left(\frac{x - \mu}{\sqrt{\sigma^2 + \epsilon}}\right)$

#### CoordAtt 实现

```python
# conv.py L378-L401
class CoordAtt(nn.Module):
    def __init__(self, c1, reduction=32):
        super().__init__()
        mid = max(c1 // reduction, 8)
        self.pool_h = nn.AdaptiveAvgPool2d((None, 1))
        self.pool_w = nn.AdaptiveAvgPool2d((1, None))
        self.conv1 = nn.Conv2d(c1, mid, 1, bias=False)
        self.bn1 = nn.BatchNorm2d(mid)
        self.act = nn.SiLU(inplace=True)
        self.conv_h = nn.Conv2d(mid, c1, 1, bias=False)
        self.conv_w = nn.Conv2d(mid, c1, 1, bias=False)

    def forward(self, x):
        b, c, h, w = x.size()
        x_h = self.pool_h(x)
        x_w = self.pool_w(x).permute(0, 1, 3, 2)
        y = torch.cat([x_h, x_w], dim=2)
        y = self.act(self.bn1(self.conv1(y)))
        x_h, x_w = y.split([h, w], dim=2)
        x_h = self.conv_h(x_h).sigmoid()
        x_w = self.conv_w(x_w.permute(0, 1, 3, 2)).sigmoid()
        return x * x_h * x_w
```

公式：$\hat{x} = x \odot \sigma(W_h \cdot f_h) \odot \sigma(W_w \cdot f_w)$

### 2.2 代码集成（三步注册链）

5 个注意力类均已完成代码注册（包括实验用的 3 个 + 备用的 2 个），注册链：

```
conv.py  (类定义: CBAM, SEAttention, ECAAttention, SimAM, CoordAtt)
  → __init__.py  (模块导出: import + __all__)
    → tasks.py  (import 到 globals + parse_model 通道适配)
```

**tasks.py 中的解析分支**（L933-L936）：

```python
elif m in {CBAM, SEAttention, ECAAttention, SimAM, CoordAtt}:
    c1 = ch[f]       # 从上一层获取实际通道数
    c2 = c1           # 注意力模块不改变通道数
    args = [c1, *args[1:]]
```

### 2.3 插入位置设计

| 位置 | 插入点 | 新增层数 | Backbone 兼容 | 适用场景 |
|------|--------|---------|-------------|---------|
| **Position A** | SPPF 后（Backbone 末尾） | 1 层 | 完全兼容 | 快速验证，最小改动 |
| **Position C** | Neck 每个 C2f 后 | 4 层 | 完全兼容 | 全面增强，效果上限更高 |

### 2.4 YAML 配置文件

| 文件 | 注意力 | 位置 | 使用方式 |
|------|--------|------|---------|
| `yolov8-seg-cbam-a.yaml` | CBAM | A | `YOLO("ultralytics/cfg/models/v8/yolov8-seg-cbam-a.yaml")` |
| `yolov8-seg-cbam-c.yaml` | CBAM | C | `YOLO("ultralytics/cfg/models/v8/yolov8-seg-cbam-c.yaml")` |
| `yolov8-seg-ca-a.yaml` | CoordAtt | A | `YOLO("ultralytics/cfg/models/v8/yolov8-seg-ca-a.yaml")` |
| `yolov8-seg-ca-c.yaml` | CoordAtt | C | `YOLO("ultralytics/cfg/models/v8/yolov8-seg-ca-c.yaml")` |
| `yolov8-seg-simam-a.yaml` | SimAM | A | `YOLO("ultralytics/cfg/models/v8/yolov8-seg-simam-a.yaml")` |

### 2.5 注意力实验设计与工具链

**训练脚本**：`cmd/yolov8-seg-attention-train.py`

```bash
# 交互选择实验
python cmd/yolov8-seg-attention-train.py

# 指定单个实验
python cmd/yolov8-seg-attention-train.py --exp cbam-a

# 批量执行全部（含自动断点恢复）
python cmd/yolov8-seg-attention-train.py --exp all --resume
```

实验矩阵（3 种注意力 × 2 个位置 + 1 基线 = 6 组）：

| 编号 | 名称 | 注意力 | 位置 | YAML | 目的 |
|------|------|--------|------|------|------|
| Exp-0 | baseline | 无 | - | 原版 yolov8n-seg | 对照组 |
| Exp-1 | cbam-a | CBAM | A（SPPF后） | yolov8-seg-cbam-a.yaml | 通道+空间注意力 |
| Exp-2 | simam-a | SimAM | A（SPPF后） | yolov8-seg-simam-a.yaml | 零参数消融对照 |
| Exp-3 | ca-a | CoordAtt | A（SPPF后） | yolov8-seg-ca-a.yaml | 位置编码效果 |
| Exp-4 | cbam-c | CBAM | C（Neck 4处） | yolov8-seg-cbam-c.yaml | Neck 全面增强 |
| Exp-5 | ca-c | CoordAtt | C（Neck 4处） | yolov8-seg-ca-c.yaml | 位置编码+多尺度 |

> 注：SimAM 未做 Position C 实验（因零参数特性，Position A 已足以验证）

**评估脚本**：`cmd/yolov8-seg-attention-val.py`

```bash
python cmd/yolov8-seg-attention-val.py              # val 集评估
python cmd/yolov8-seg-attention-val.py --split test  # test 集评估
```

自动产出 3 张对比表格（CSV）：
- `table1_overview.csv` — 各实验核心指标总览
- `table2_ablation_type.csv` — 注意力类型消融（位置 A 固定，对比 CBAM/SimAM/CA）
- `table3_ablation_pos.csv` — 插入位置消融（A vs C）

### 2.6 注意力实验结果

| 实验 | 配置 | mAP@0.5 | mAP@0.5:0.95 | 结论 |
|------|------|---------|-------------|------|
| baseline | 原版 yolov8n-seg | 基准 | 基准 | — |
| CoordAtt-A | SPPF 后 1 层 CA | ≈ 持平 | ↓ 0.05~0.1 | 未提升 |
| CoordAtt-C | Neck 4 层 CA | ≈ 持平 | ↓ 0.05~0.1 | 未提升 |
| CBAM-A | SPPF 后 1 层 CBAM | ≈ 持平 | ↓ 0.05~0.1 | 未提升 |
| CBAM-C | Neck 4 层 CBAM | ≈ 持平 | ↓ 0.05~0.1 | 未提升 |
| SimAM-A | SPPF 后 1 层 SimAM | ≈ 持平 | ↓ 0.05~0.1 | 未提升 |

### 2.7 注意力效果不佳的原因分析

1. **单类别任务过于简单**：分类和定位难度低，"选择性增强"优势无法发挥
2. **nano 模型容量有限**：3.41M 参数，额外注意力在小数据集上引入过拟合风险
3. **mAP@0.5 持平但 mAP@0.5:0.95 下降**：粗略定位不受影响，但精细定位变差——注意力模块在高 IoU 阈值下引入了特征对齐噪声
4. **结论**：注意力机制的实验结果本身也是论文的重要贡献——证明了"并非所有改进都适用于所有场景"

### 2.8 参数量对比

| 配置 | 总参数量 | 增量 | 增幅 |
|------|---------|------|------|
| baseline | 3,409,968 | - | - |
| CBAM + A | 3,475,858 | +65,890 | +1.93% |
| SimAM + A | 3,409,968 | +0 | +0.00% |
| CoordAtt + A | 3,416,128 | +6,160 | +0.18% |
| CBAM + C | 3,513,336 | +103,368 | +3.03% |
| CoordAtt + C | 3,423,856 | +13,888 | +0.41% |

---

## 三、改进技术二：IoU 损失改进（已完成实施与实验）

> 训练脚本：`cmd/yolov8-seg-loss-train.py`（统一管理 IoU 损失 + 掩码损失实验）
> 评估脚本：`cmd/yolov8-seg-loss-val.py`

### 3.1 原始实现

**文件**：`ultralytics/utils/metrics.py` L75-L131

`bbox_iou` 函数支持 IoU/GIoU/DIoU/CIoU。在 `BboxLoss.forward`（`loss.py` L76）中固定 `CIoU=True`。

CIoU 公式：

$$\mathcal{L}_{CIoU} = 1 - \text{IoU} + \frac{\rho^2(b, b^{gt})}{c^2} + \alpha v, \quad v = \frac{4}{\pi^2}\left(\arctan\frac{w^{gt}}{h^{gt}} - \arctan\frac{w}{h}\right)^2$$

**CIoU 的不足**：宽高比通过 arctan 比值耦合；对所有样本统一权重；中心距离无方向性。

### 3.2 方案 A1：EIoU（Efficient IoU, 2022）

**论文**：Focal and Efficient IOU Loss for Accurate Bounding Box Regression

$$\mathcal{L}_{EIoU} = 1 - \text{IoU} + \frac{\rho^2(b, b^{gt})}{c^2} + \frac{(w - w^{gt})^2}{c_w^2} + \frac{(h - h^{gt})^2}{c_h^2}$$

**优势**：将宽度差和高度差分开惩罚。对快递单（矩形目标）宽和高可独立优化。不增加参数。

**实现代码**：

```python
if EIoU:
    rho_w2 = (w2 - w1).pow(2)
    rho_h2 = (h2 - h1).pow(2)
    cw2 = cw.pow(2) + eps
    ch2 = ch.pow(2) + eps
    return iou - (rho2 / c2 + rho_w2 / cw2 + rho_h2 / ch2)
```

### 3.3 方案 A2：SIoU（Scylla IoU, 2022）

**论文**：SIoU Loss: More Powerful Learning for Bounding Box Regression

$$\mathcal{L}_{SIoU} = 1 - \text{IoU} + \frac{\Delta_{dist} + \Omega_{shape}}{2}$$

包含角度感知（让预测框沿最短路径趋近 GT）、距离感知、形状感知三个组件。

**优势**：方向感知减少训练震荡，加速收敛。独立约束宽高。

**实现代码**（含数值稳定性修复）：

```python
if SIoU:
    s_cw = (b2_x1 + b2_x2 - b1_x1 - b1_x2) * 0.5
    s_ch = (b2_y1 + b2_y2 - b1_y1 - b1_y2) * 0.5
    sigma = (s_cw.pow(2) + s_ch.pow(2)).clamp(min=eps).sqrt()
    # 修复: clamp(1-1e-4) + 恒等式替代 asin，消除梯度奇点
    sin_alpha = (s_ch.abs() / sigma).clamp(0, 1.0 - 1e-4)
    angle_cost = 2 * sin_alpha * (1 - sin_alpha.pow(2)).clamp(min=0).sqrt()

    rho_x = (s_cw / cw.clamp(min=eps)).pow(2)
    rho_y = (s_ch / ch.clamp(min=eps)).pow(2)
    gamma = angle_cost - 2
    # 修复: exp 指数截断，防止极端梯度
    dist_cost = 2 - torch.exp((gamma * rho_x).clamp(min=-20))
                   - torch.exp((gamma * rho_y).clamp(min=-20))

    omiga_w = (w1 - w2).abs() / w1.maximum(w2).clamp(min=eps)
    omiga_h = (h1 - h2).abs() / h1.maximum(h2).clamp(min=eps)
    shape_cost = (1 - torch.exp(-omiga_w)).pow(4) + (1 - torch.exp(-omiga_h)).pow(4)

    return iou - 0.5 * (dist_cost + shape_cost)
```

> **数值稳定性修复说明**：原始 SIoU 实现使用 `torch.asin(sin_alpha)`，当 `sin_alpha → 1.0` 时梯度趋于无穷（导数为 $1/\sqrt{1-x^2}$），导致训练在 epoch 6 即出现 loss 发散和 `val/cls_loss = inf`。修复方案：(1) 用数学恒等式 $\cos(2\arcsin x - \pi/2) = 2x\sqrt{1-x^2}$ 完全消除 `asin`；(2) 对 `exp(gamma * rho)` 的指数做 `clamp(min=-20)` 截断。

### 3.4 方案 A3：WIoU（Wise IoU, 2023）

**论文**：Wise-IoU: Bounding Box Regression Loss with Dynamic Focusing Mechanism

$$\mathcal{L}_{WIoU} = r_{WIoU} \cdot \mathcal{L}_{IoU}, \quad r_{WIoU} = \exp\left(\frac{(x-x^{gt})^2 + (y-y^{gt})^2}{(W_g^2+H_g^2)^*}\right)$$

v3 版本的非单调聚焦：$r = \frac{\beta^*}{\delta} \cdot r_{WIoU}$，其中 $\beta^*$ 是 IoU Loss 的移动平均。

**优势**：自动降低异常样本（错误标注、边缘模糊）权重。对小数据集特别有效。聚焦中等难度样本。

**实现代码**（位于 `loss.py` BboxLoss.forward，而非 `metrics.py`，因为 WIoU 修改的是 loss 加权策略）：

```python
if self.iou_type == "WIoU":
    iou = bbox_iou(pred_bboxes[fg_mask], target_bboxes[fg_mask], xywh=False)
    with torch.no_grad():  # wise_scale 全部 detach
        p, t = pred_bboxes[fg_mask], target_bboxes[fg_mask]
        center_dist2 = ((p[:, 0:1] + p[:, 2:3] - t[:, 0:1] - t[:, 2:3]).pow(2)
                        + (p[:, 1:2] + p[:, 3:4] - t[:, 1:2] - t[:, 3:4]).pow(2)) / 4
        cw = p[:, 2:3].maximum(t[:, 2:3]) - p[:, 0:1].minimum(t[:, 0:1])
        ch = p[:, 3:4].maximum(t[:, 3:4]) - p[:, 1:2].minimum(t[:, 1:2])
        c2 = cw.pow(2) + ch.pow(2) + 1e-7
        wise_scale = torch.exp(center_dist2 / c2)
    loss_iou = ((1.0 - iou) * wise_scale * weight).sum() / target_scores_sum
```

> 实现的是 WIoU v1 版本。v3 需要 EMA 维护 loss 移动平均，复杂度更高，v1 已具备动态聚焦的核心能力。

### 3.5 按需加载设计

**新增配置**（`default.yaml`）：

```yaml
iou_type: CIoU       # 可选: IoU, GIoU, DIoU, CIoU, EIoU, SIoU, WIoU
seg_loss_type: BCE    # 可选: BCE, Dice, BCE-Dice
```

**已修改文件**：

| 文件 | 修改点 |
|------|--------|
| `ultralytics/utils/metrics.py` | `bbox_iou()` 增加 `EIoU=False, SIoU=False` 参数和对应分支 |
| `ultralytics/utils/loss.py` | `BboxLoss.__init__` 接受 `iou_type` 参数 |
| `ultralytics/utils/loss.py` | `BboxLoss.forward` 按 iou_type 分发调用（含 WIoU 独立分支） |
| `ultralytics/utils/loss.py` | `v8DetectionLoss.__init__` 从 `self.hyp` 读取 iou_type 传递给 BboxLoss |
| `ultralytics/cfg/default.yaml` | 新增 `iou_type: CIoU` 和 `seg_loss_type: BCE` |

**使用方式**：

```bash
# 默认（CIoU + BCE，不受影响）
yolo train model=yolov8n-seg.yaml data=yolov8-bvn.yaml

# 使用 EIoU
yolo train model=yolov8n-seg.yaml data=yolov8-bvn.yaml iou_type=EIoU

# 使用 SIoU（--box 5.0 建议）
yolo train model=yolov8n-seg.yaml data=yolov8-bvn.yaml iou_type=SIoU
```

### 3.6 三种 IoU 对比

| 方法 | 年份 | 核心改进 | 对快递单检测的意义 | 实现复杂度 |
|------|------|---------|----------------|-----------|
| **EIoU** | 2022 | 宽高分开惩罚 | 矩形运单的宽高可独立优化 | 低 |
| **SIoU** | 2022 | 角度感知 + 形状感知 | 减少优化震荡，加速收敛 | 中 |
| **WIoU** | 2023 | 动态样本权重 | 降低模糊边缘/错误标注影响 | 中高 |

### 3.7 IoU 损失实验结果

| 实验 | IoU 类型 | mAP@0.5 | mAP@0.5:0.95 | 结论 |
|------|---------|---------|-------------|------|
| baseline | CIoU | 基准 | 基准 | 对照组 |
| eiou | EIoU | ≈ 持平 | ↓ 略低 | 宽高分离惩罚在简单目标上无额外收益 |
| wiou | WIoU | ≈ 持平 | ↓ 略低 | 动态聚焦在小数据集上无显著优势 |
| siou | SIoU | — | 训练发散 | 原始实现有数值稳定性 bug，已修复待重跑 |

### 3.8 IoU 损失效果分析

IoU 损失改进主要针对**框回归**质量，但在本任务中效果有限：

1. **CIoU 已是近最优**：YOLOv8 默认的 `box=7.5` 等超参数为 CIoU 深度调优，其他 loss 使用相同超参存在天然劣势
2. **单类简单目标**：快递单形状规则、尺度变化有限，框回归难度低，高级 IoU 的优势在困难场景（小目标、密集目标）更明显
3. **分割任务的评测指标（mAP@0.5:0.95）基于掩码 IoU**，而非框 IoU——改进框回归对最终指标的影响是间接的

> 这一结论本身是论文的有价值发现：**IoU 损失改进的有效性与任务复杂度相关，在简单场景中默认 CIoU 已是近最优选择。**

---

## 四、改进技术三：分割掩码损失改进（已完成实施）

> 与 IoU 损失实验共用同一套脚本：`cmd/yolov8-seg-loss-train.py` / `cmd/yolov8-seg-loss-val.py`

### 4.1 原始实现

**文件**：`ultralytics/utils/loss.py` `single_mask_loss()`

```python
pred_mask = torch.einsum("in,nhw->ihw", pred, proto)
loss = F.binary_cross_entropy_with_logits(pred_mask, gt_mask, reduction="none")
return (crop_mask(loss, xyxy).mean(dim=(1, 2)) / area).sum()
```

**BCE 的三个关键缺陷**：

| 缺陷 | 说明 | 对 mAP@0.5:0.95 的影响 |
|------|------|----------------------|
| 像素独立 | 每个像素单独计算损失，不感知区域整体重叠 | 无法直接优化掩码 IoU |
| 前景稀疏 | 快递单像素占比 < 20%，梯度被背景主导 | 边缘预测偏保守 |
| 边界不敏感 | 边界像素与内部像素权重相同 | 高 IoU 阈值下精度不足 |

### 4.2 改进方案：Dice Loss + BCE 组合

**来源**：V-Net（Milletari et al., 2016）

$$\mathcal{L}_{Dice} = 1 - \frac{2 \sum p_i g_i + \epsilon}{\sum p_i + \sum g_i + \epsilon}$$

**三种配置模式**：

| seg_loss_type | 公式 | 特点 |
|--------------|------|------|
| `BCE` | $L_{BCE}$ | 默认，逐像素交叉熵 |
| `Dice` | $L_{Dice}$ | 纯区域重叠优化 |
| `BCE-Dice` | $L_{BCE} + L_{Dice}$ | **推荐** — 像素级 + 区域级联合 |

**BCE-Dice 互补原理**：
- **BCE** 提供稳定的逐像素梯度信号，保障训练稳定性和边缘细节
- **Dice** 直接优化预测与 GT 的全局重叠比（等价于 soft IoU），与 mAP 评价指标天然对齐
- 两者联合：BCE 管精度，Dice 管覆盖率

### 4.3 实现代码

**Dice Loss 函数**（`loss.py` 顶部新增）：

```python
def dice_loss(pred_mask, gt_mask, smooth=1.0):
    pred = pred_mask.sigmoid()
    pred_flat = pred.flatten(1)
    gt_flat = gt_mask.flatten(1)
    intersection = (pred_flat * gt_flat).sum(1)
    return 1 - ((2.0 * intersection + smooth) / (pred_flat.sum(1) + gt_flat.sum(1) + smooth)).mean()
```

**single_mask_loss 改造**：

```python
@staticmethod
def single_mask_loss(gt_mask, pred, proto, xyxy, area, seg_loss_type="BCE"):
    pred_mask = torch.einsum("in,nhw->ihw", pred, proto)

    if seg_loss_type == "Dice":
        return dice_loss(crop_mask(pred_mask, xyxy), crop_mask(gt_mask, xyxy))

    bce = F.binary_cross_entropy_with_logits(pred_mask, gt_mask, reduction="none")
    bce_loss = (crop_mask(bce, xyxy).mean(dim=(1, 2)) / area).sum()

    if seg_loss_type == "BCE-Dice":
        return bce_loss + dice_loss(crop_mask(pred_mask, xyxy), crop_mask(gt_mask, xyxy))

    return bce_loss  # 默认 BCE
```

### 4.4 按需加载设计

**配置**（`default.yaml`）：

```yaml
seg_loss_type: BCE  # 可选: BCE, Dice, BCE-Dice
```

**已修改文件**：

| 文件 | 修改点 |
|------|--------|
| `ultralytics/utils/loss.py` | 新增 `dice_loss()` 全局函数 |
| `ultralytics/utils/loss.py` | `single_mask_loss` 增加 `seg_loss_type` 参数，支持 BCE/Dice/BCE-Dice |
| `ultralytics/utils/loss.py` | `v8SegmentationLoss.__init__` 从 hyp 读取 `seg_loss_type` |
| `ultralytics/utils/loss.py` | `calculate_segmentation_loss` 将 `seg_loss_type` 传递给 `single_mask_loss` |
| `ultralytics/cfg/default.yaml` | 新增 `seg_loss_type: BCE` |

**使用方式**：

```bash
# 默认 BCE（不受影响）
yolo train model=yolov8n-seg.yaml data=yolov8-bvn.yaml

# 使用 Dice Loss
yolo train model=yolov8n-seg.yaml data=yolov8-bvn.yaml seg_loss_type=Dice

# 使用 BCE + Dice（推荐）
yolo train model=yolov8n-seg.yaml data=yolov8-bvn.yaml seg_loss_type=BCE-Dice
```

---

## 五、改进技术四：标签分配策略调优（待实施）

### 5.1 当前实现

**文件**：`ultralytics/utils/loss.py` L167

```python
self.assigner = TaskAlignedAssigner(topk=10, num_classes=self.nc, alpha=0.5, beta=6.0)
```

对齐度量：$\text{align} = s^\alpha \times u^\beta$

### 5.2 单类别场景的问题

nc=1 时分类分数 $s$ 区分度极低（只有 1 类，很快趋近 1.0），$s^{0.5} \approx 1.0$，导致匹配几乎只看 IoU。topk=10 可能引入过多低质量正样本。

### 5.3 调优建议

```python
self.assigner = TaskAlignedAssigner(topk=5, num_classes=self.nc, alpha=0.3, beta=8.0)
```

- **topk: 10→5**：单类别不需要 10 个候选，减少噪声
- **alpha: 0.5→0.3**：降低低区分度的分类分数影响
- **beta: 6.0→8.0**：增加 IoU 质量权重，提升定位精度

### 5.4 按需加载设计

```yaml
tal_topk: 10     # 默认
tal_alpha: 0.5   # 默认
tal_beta: 6.0    # 默认
```

---

## 六、实验设计（统一框架）

### 6.1 完整消融实验矩阵

**第一组：注意力机制消融（已有工具链支持）**

| 编号 | 配置 | IoU | Mask Loss | 目的 |
|------|------|-----|-----------|------|
| ATT-0 | baseline | CIoU | BCE | 对照组 |
| ATT-1 | CBAM + A | CIoU | BCE | 通道+空间注意力 |
| ATT-2 | SimAM + A | CIoU | BCE | 零参数消融对照 |
| ATT-3 | CA + A | CIoU | BCE | 位置编码效果 |
| ATT-4 | CBAM + C | CIoU | BCE | Neck 全面增强 |
| ATT-5 | CA + C | CIoU | BCE | 位置编码+多尺度 |

**第二组：损失函数消融（已完成工具链）**

脚本：`cmd/yolov8-seg-loss-train.py` / `cmd/yolov8-seg-loss-val.py`

| 编号 | 实验名 | IoU | Mask Loss | 目的 | 状态 |
|------|--------|-----|-----------|------|------|
| LOSS-0 | baseline | CIoU | BCE | 对照组（复用 ATT-0） | 已完成 |
| LOSS-1 | eiou | **EIoU** | BCE | 宽高分离惩罚 | 已完成 |
| LOSS-2 | siou | **SIoU** | BCE | 角度+形状感知 | 待重跑（已修复数值bug） |
| LOSS-3 | wiou | **WIoU** | BCE | 动态聚焦加权 | 已完成 |
| LOSS-4 | dice | CIoU | **Dice** | 纯区域重叠优化 | 待训练 |
| LOSS-5 | bce-dice | CIoU | **BCE+Dice** | 像素+区域联合 | 待训练 |

```bash
# 训练（跳过已有 baseline，从注意力实验复用）
python cmd/yolov8-seg-loss-train.py --exp all --skip-baseline --resume

# 只跑掩码损失实验
python cmd/yolov8-seg-loss-train.py --exp dice
python cmd/yolov8-seg-loss-train.py --exp bce-dice

# 评估 → 自动输出 3 张对比表格
python cmd/yolov8-seg-loss-val.py
```

评估产出：
- `table1_overview.csv` — 所有损失实验总览
- `table2_iou_ablation.csv` — IoU 损失消融（baseline vs EIoU/SIoU/WIoU）
- `table3_mask_ablation.csv` — 掩码损失消融（baseline vs Dice/BCE-Dice）

### 6.2 统一训练配置

```yaml
model: yolov8n-seg
imgsz: 960
epochs: 500          # 用 patience 控制实际停止
batch: 16
patience: 50
optimizer: auto
workers: 0           # Windows 兼容
```

### 6.3 评估指标

| 指标 | 含义 | 重要性 |
|------|------|--------|
| **mask mAP@0.5:0.95** | 掩码多阈值精度 | 最重要 |
| **mask mAP@0.5** | 掩码 AP@0.5 | 核心 |
| box mAP@0.5:0.95 | 检测框精度 | 辅助 |
| box mAP@0.5 | 检测框 AP@0.5 | 辅助 |
| Params (M) | 模型参数量 | 轻量化评价 |
| FLOPs (G) | 浮点运算量 | 效率评价 |
| Inference (ms) | 推理耗时 | 实时性评价 |

### 6.4 可重复性

- 每组实验用 3 个随机种子（42、123、2024）跑 3 次，取均值 ± 标准差
- 记录训练时间和 GPU 显存

---

## 七、论文结构建议

### 7.1 推荐标题

> 中文：基于改进损失函数的 YOLOv8 快递单实例分割方法
>
> 英文：An Improved YOLOv8-based Instance Segmentation Method with Enhanced Loss Functions for Express Label Detection

### 7.2 章节结构

```
摘要
1. 引言
  1.1 研究背景（快递物流自动化）
  1.2 相关工作（YOLO 系列、IoU 损失演进、实例分割、注意力机制）
  1.3 本文贡献

2. YOLOv8 架构分析
  2.1 整体架构（Backbone + Neck + Head）
  2.2 分割头原理（Proto + 掩码系数 → 实例掩码）
  2.3 现有损失函数分析及不足

3. 改进方法
  3.1 改进一：XX IoU 损失（公式推导、优势分析）
  3.2 改进二：BCE + Dice 组合掩码损失
  3.3 改进三（可选）：标签分配策略调优
  3.4 整体改进框架图

4. 实验
  4.1 数据集描述
  4.2 实验设置
  4.3 消融实验
    - 表1: 不同 IoU 损失对比
    - 表2: 掩码损失对比
    - 表3: 组合效果
  4.4 注意力机制对比实验
    - 表4: 多种注意力机制效果
    - 分析: 为什么注意力在单类别小数据集上效果有限
  4.5 与其他方法对比（YOLOv5-seg、YOLOv8s-seg 等）
  4.6 可视化分析
  4.7 参数量与推理速度

5. 结论与展望

参考文献
```

### 7.3 本文贡献写法建议

```
（1）针对快递单检测中边界框回归精度不足的问题，提出使用 XX IoU 损失替代
    传统 CIoU 损失，通过 XX 机制提升了高 IoU 阈值下的定位精度。
（2）针对实例分割中前景/背景不均衡的问题，引入 Dice Loss 与 BCE Loss 的
    组合掩码损失，改善了快递单边缘的分割质量。
（3）系统比较了多种注意力机制（CBAM、CoordAtt、SimAM）和损失函数改进在
    单类别小数据集场景下的效果，为类似任务提供了参考。
（4）在自建快递单数据集上进行了充分的消融实验和对比实验，验证了方法有效性。
```

---

## 八、实施计划

| 阶段 | 任务 | 预计时间 | 状态 |
|------|------|---------|------|
| 1 | 实现 EIoU/SIoU/WIoU（metrics.py + loss.py） | 1~2 天 | **已完成**（含 SIoU 数值修复） |
| 2 | 实现 Dice Loss（loss.py） | 1 天 | **已完成** |
| 3 | 编写统一损失实验脚本 | 1 天 | **已完成**（训练 + 评估） |
| 4 | EIoU/WIoU 消融实验 | 3~5 天 | **已完成**（mAP95 略低于 baseline） |
| 5 | SIoU 重跑（修复后） | 1~2 天 | 待执行 |
| 6 | Dice / BCE-Dice 实验 | 2~3 天 | **待训练**（代码就绪） |
| 7 | 实现 Assigner 可配置化 | 0.5 天 | 待实施 |
| 8 | 可视化分析 | 1~2 天 | 待执行 |
| 9 | 论文写作 | 5~7 天 | 待执行 |

---

## 附录 A：参考文献

| 方法 | 论文 | 年份 |
|------|------|------|
| YOLOv8 | Ultralytics YOLOv8 | 2023 |
| CIoU | Distance-IoU Loss: Faster and Better Learning for Bounding Box Regression | 2020 |
| EIoU | Focal and Efficient IOU Loss for Accurate Bounding Box Regression | 2022 |
| SIoU | SIoU Loss: More Powerful Learning for Bounding Box Regression | 2022 |
| WIoU | Wise-IoU: Bounding Box Regression Loss with Dynamic Focusing Mechanism | 2023 |
| Dice Loss | V-Net: Fully Convolutional Neural Networks for Volumetric Medical Image Segmentation | 2016 |
| CBAM | CBAM: Convolutional Block Attention Module, ECCV 2018 |
| SE-Net | Squeeze-and-Excitation Networks, CVPR 2018 |
| ECA-Net | ECA-Net: Efficient Channel Attention, CVPR 2020 |
| SimAM | SimAM: A Simple, Parameter-Free Attention Module, ICML 2021 |
| CoordAtt | Coordinate Attention for Efficient Mobile Network Design, CVPR 2021 |

## 附录 B：完整文件索引

### 文档文件

| 文件 | 内容 |
|------|------|
| `docs/技术改进方案.md` | **本文档** — 统一技术参考 |
| `docs/attention-analysis.md` | 6 种注意力机制详解 + 4 种插入位置分析 + 代码集成指南 |
| `docs/attention-integration-doc.md` | 论文方法论写作参考 + 实验设计 + 参数量统计 |

### 脚本文件

| 文件 | 功能 |
|------|------|
| `cmd/yolov8-seg-attention-train.py` | 注意力实验批量训练（6 组，支持断点恢复） |
| `cmd/yolov8-seg-attention-val.py` | 注意力实验统一评估（自动输出 3 张 CSV 表格） |
| `cmd/yolov8-seg-loss-train.py` | **损失函数实验批量训练**（IoU + 掩码损失，6 组 + baseline，支持 `--skip-baseline`、`--box`、`--resume`） |
| `cmd/yolov8-seg-loss-val.py` | **损失函数实验统一评估**（自动输出 3 张 CSV：总览、IoU 消融、掩码消融） |
| `cmd/yolov8-seg-train.py` | 基线训练脚本 |
| `cmd/yolov8-seg-finetune.py` | 场景补充微调（4 种预设模式） |
| `cmd/yolov8-seg-val.py` | 通用评估脚本 |
| `cmd/yolov8-seg-predict.py` | 推理预测脚本 |
| `cmd/yolov8-seg-resume.py` | 断点恢复训练 |
| `cmd/yolov8-seg-export.py` | 模型导出 |
| `cmd/batch_image_augmentation.py` | 批量数据增强 |
| `cmd/coco2yolo_seg.py` | COCO 格式转 YOLO 分割格式 |
| `cmd/batch_rename.py` | 批量重命名 |
| `cmd/demo_predict.py` | 演示推理 |

### 模型配置文件

| 文件 | 说明 |
|------|------|
| `ultralytics/cfg/models/v8/yolov8-seg.yaml` | 原版基线（不修改） |
| `ultralytics/cfg/models/v8/yolov8-seg-cbam-a.yaml` | CBAM + Position A |
| `ultralytics/cfg/models/v8/yolov8-seg-cbam-c.yaml` | CBAM + Position C |
| `ultralytics/cfg/models/v8/yolov8-seg-ca-a.yaml` | CoordAtt + Position A |
| `ultralytics/cfg/models/v8/yolov8-seg-ca-c.yaml` | CoordAtt + Position C |
| `ultralytics/cfg/models/v8/yolov8-seg-simam-a.yaml` | SimAM + Position A |

### 关键源代码位置

| 功能 | 文件 | 说明 |
|------|------|------|
| 注意力模块定义 | `ultralytics/nn/modules/conv.py` | CBAM, SimAM, CoordAtt 等 5 个类 |
| 模块导出 | `ultralytics/nn/modules/__init__.py` | import + \_\_all\_\_ |
| 模型解析（注意力分支） | `ultralytics/nn/tasks.py` | parse_model 中的通道适配 |
| IoU 计算 | `ultralytics/utils/metrics.py` | bbox_iou()：IoU/GIoU/DIoU/CIoU/EIoU/SIoU |
| Dice Loss | `ultralytics/utils/loss.py` | dice_loss() 顶层函数 |
| Box 损失 | `ultralytics/utils/loss.py` | BboxLoss（含 WIoU 加权分支） |
| 掩码损失 | `ultralytics/utils/loss.py` | single_mask_loss()：BCE/Dice/BCE-Dice |
| 分割总损失 | `ultralytics/utils/loss.py` | v8SegmentationLoss 类 |
| 标签分配器初始化 | `ultralytics/utils/loss.py` | v8DetectionLoss.__init__ |
| TaskAligned Assigner | `ultralytics/utils/tal.py` | TaskAlignedAssigner 类 |
| 训练超参数 | `ultralytics/cfg/default.yaml` | iou_type, seg_loss_type 等 |

### 训练产出目录

```
runs/segment/
├── train*/                    # 基线训练历史
├── attnv2_baseline/           # 注意力对照组（也作为损失实验 baseline）
├── attnv2_cbam-a/             # CBAM + A
├── attnv2_simam-a/            # SimAM + A
├── attnv2_ca-a/               # CoordAtt + A
├── attnv2_cbam-c/             # CBAM + C
├── attnv2_ca-c/               # CoordAtt + C
├── attnv2_analysis_{split}/   # 注意力评估结果（CSV）
├── loss_eiou/                 # EIoU 实验（已完成）
├── loss_siou/                 # SIoU 实验（已修复，待重跑）
├── loss_wiou/                 # WIoU 实验（已完成）
├── loss_dice/                 # Dice Loss 实验（待训练）
├── loss_bce-dice/             # BCE+Dice 实验（待训练）
└── loss_analysis_{split}/     # 损失函数评估结果（3 张 CSV）
```
